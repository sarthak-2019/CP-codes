void solve()
{
  int val, num;
  read(num, val);
  int coins[num];
  for (int i = 0; i < num; i++) {
    read(coins[i]);
  }
  int dp[val + 1];
  mem(dp, 0);
  for (int i = 1; i <= val; i++) {
    int t = inf;
    for (int j = 0; j < num; j++) {
      if (i - coins[j] >= 0) {
        t = min(t, dp[i - coins[j]]);
      }
    }
    dp[i] = t + 1;
  }
  cout << dp[val];
}
